"""The arms. SPEC §14.1.

Each is one `choose()` method. Baselines are given full capability and denied
only intelligence: same channels, same templates, same e-mandate notice. A
baseline crippled by omission produces a fake win, which is the easiest way for
a recovery product to flatter itself and the first thing a judge will look for.
"""

# HYBRID is the exception to "each is one choose() method": it has one, but the
# method only picks which of two existing arms answers. It composes; it does not
# decide. See settle/runner/arms/hybrid.py.

__all__ = []
